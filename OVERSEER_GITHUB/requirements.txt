# OVERSEER — Python deps for running from source (Python 3.11+)
flask>=3.0.0
psutil>=5.9.0
pywebview>=5.0
numpy>=1.24.0
soundcard>=0.4.0
# Optional Windows loopback backend (used by RobCo Broadcast Relay)
PyAudioWPatch>=0.2.12.6
# Optional NVIDIA GPU metrics
# nvidia-ml-py>=12.0.0
# Packaging (dev only — not needed to run from source)
# pyinstaller>=6.0
